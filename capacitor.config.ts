import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'id.ygbm.koneksi',
  appName: 'YGBM KONEKSI',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
